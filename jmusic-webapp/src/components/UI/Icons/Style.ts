import styled from 'styled-components';

export const StyledIconContent = styled.div`
  width: 24px;
  height: 24px;
  margin: 0 10px;
  cursor: pointer;

  .jmusic-icon {
    fill: #fff;
  }

  &:hover .jmusic-icon {
    fill: #94DAA0;
  }
`;
